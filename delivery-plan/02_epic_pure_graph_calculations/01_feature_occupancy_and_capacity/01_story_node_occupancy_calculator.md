# Gleisbelegungsrechner

**Umsetzung:** Pure Funktion aus Knoten, Zuglaeufen und optionaler Knotenressource. Ergebnis: Slots, zeitliche Verteilung, Mindestbedarf und optional Kapazitaet, Auslastung, `sufficient`/`tight`/`overloaded`.

**Test:** Parallele Aufenthalte, fehlende Ressource und Kapazitaetsgrenzen. **UI:** nicht anwendbar.

**Commit:** `refactor(analytics): extract pure node occupancy calculator`