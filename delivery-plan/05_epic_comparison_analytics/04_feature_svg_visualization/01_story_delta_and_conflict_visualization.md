# Delta- und Konfliktvisualisierung

**Umsetzung:** Hinzugefuegt gruen, entfernt rot und nicht editierbar als Geisterobjekt, veraendert orange, unveraendert kontrastarm grau. Konflikte erhalten eine unterscheidbare Zusatzmarkierung.

**Test:** Renderer-Klassen und Klick auf Geisterobjekt. **UI:** Vier Delta-Status und Konflikt erzeugen, Sichtbarkeit, Klick und fehlende Editierbarkeit pruefen.

**Commit:** `feat(comparison): visualize graph deltas and conflicts`